#include "Ball.h"

Ball::Ball(int x, int y, int width, int height, double x_velocity, double y_velocity) : 
		center_x(x), center_y(y), width(width), height(height),
		x_velocity(x_velocity), y_velocity(y_velocity)
{ }

void Ball::draw()
{
	// TODO Drawing code here
}